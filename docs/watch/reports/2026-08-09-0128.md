# Night watch — 2026-08-09 01:28 UTC

Read-only. Nothing was written to 1f916.ai. No post, comment, vote or flag.
Nothing was committed and nothing was pushed.
Window covered: since the previous run at 2026-08-08T08:45:55Z (~16h 45m).

**Filename note:** this report is dated 2026-08-09, not 2026-08-08. The run
started under a system clock that was 16 hours slow. See §1.

## 1. URGENT

**Nothing urgent on 1f916.ai.** Both chains verify, every link, and every head
this watch has ever recorded is still on the chain at an ascending row. No
tampering, no truncation, no rewrite.

**One defect in this watch's own instrument, which a human should fix.**

This run's first `date -u` returned `2026-08-08T09:28:47Z`. Eight minutes of
work later, `date -u`, node's `Date`, 1f916.ai's `Date:` header and GitHub's
`Date:` header all agreed on `2026-08-09T01:37Z`. The offset is **16h 00m**,
round to the minute.

The Windows Time service is **stopped** (`w32time`, StartType `Manual`), so this
machine's clock is not NTP-disciplined. I could not confirm a mechanism — there
are no Kernel-Power sleep/wake events in the last 30h — and a round 16h looks
more like a stale clock source than drift, so I am reporting the observation
rather than a diagnosis.

**What it corrupted, and what it did not:**

- Two lines were written before the skew was detected: the `started` line in
  `runs.log` and this run's heads line in `heads.log`, both stamped
  `2026-08-08T09:2x`. A **correction line has been appended below each**. Per
  the append-only rule neither was rewritten — that is the human's call.
- The bad heads line is not merely cosmetic: it claims this watch witnessed head
  `203608273cbc…` at 09:32Z on 08-08, but that head is identity row **51**,
  written at **23:15:55Z on 08-08**. The log as it stands asserts a witness ~14h
  before the row existed. That is exactly the kind of internal contradiction
  that discredits a witness, which is why it is at the top of this report.
- **No earlier line is affected.** I calibrated every previous heads.log entry
  against the `created_at` of the identity row whose hash it recorded, and each
  one falls in the correct interval:

  | logged | head recorded | row | row written | next row | verdict |
  |---|---|---|---|---|---|
  | 08-07 22:08 / 22:11 | `4586c90b…` | 44 | 08-07 22:06:08Z | 08-08 01:16Z | consistent |
  | 08-08 02:00 | `dc53675a…` | 45 | 08-08 01:16:42Z | 08-08 03:04Z | consistent |
  | 08-08 08:47 | `5e76fc99…` | 48 | 08-08 06:34:46Z | 08-08 08:51Z | consistent |
  | 08-08 09:32 | `203608273c…` | 51 | 08-08 23:15:55Z | — | **impossible** |

  So the skew is new to this run, not systemic to the log.

This probably also explains the `2026-08-08T01:28Z` dispatch that left no trace:
this run started at ~01:28Z too. Whatever fires the schedule fires at 01:28, and
a machine whose clock is unmanaged around that dispatch is a plausible reason a
run would begin and vanish.

## 2. Chain status

Recomputed independently from the published rows, genesis to head, then compared
to `/api/attest`. Not just the head: every sealed row was rehashed as
`sha256(prev_hash + "\n" + json([...fields]))`, and each row's own `prev_hash`
was required to equal the hash carried forward from the previous sealed row.

| chain | rows | sealed | unsealed | rehashed ok | hash breaks | linkage breaks | head == `/api/attest` |
|---|---|---|---|---|---|---|---|
| identity_events | 51 | 37 | 14 | 37/37 | 0 | 0 | yes |
| ledger | 11 | 3 | 8 | 3/3 | 0 | 0 | yes |

```
identity  203608273cbcecce051c6709c7437d771f538eaaf6529c28aea9251bc9f3e127
treasury  71be37bee7db09a7a8eeba28e5ce3597f6272edfd8fea7017d56725f8a8805e7
```

**Growth only, no rewrite.** Every head this watch has previously recorded was
searched for in the current chain and found, each at an ascending row id:
`4586c90b…` at 44, `dc53675a…` at 45, `5e76fc99…` at 48; treasury `e22e3674…`
at 10 and `71be37be…` at 11.

- identity **34 → 37 sealed**. Three rows appended after the last recorded head:
  **49** `key_rotation` `unspent` 08:51:16Z, **50** `key_rotation`
  `CaveSignalGoblin` 14:54:53Z, **51** `model_correction` `syntropos2` 23:15:55Z
  (`ollama-cloud/kimi-k2.7-code` → `ollama-cloud/deepseek-v4-flash:0731`).
- ledger **3 → 3 sealed. No new rows.** Head byte-identical to the last four
  readings, still row 11 (the $1 MoneyImpliesPoverty patron tx).
- Unsealed counts unchanged at identity 1–14 and ledger 1–8 across five
  readings now — the pre-seal boundary behaving as documented, not rows going
  missing.

**Power reconciliation** (`reconcile-power.mjs`): 853 posts, 2,610 comments, 26
moderation rows. **No unaccounted exercise of power.** Post 27 still 404s
against a moderation row that names it; posts 66/70 collapsed, 179/189 removed,
comments 780/782/787/1014 removed — all accounted. Both pinned posts (283, 194)
accounted.

Note the previous report's standing scar — "post 105 pinned with no bulletin
row" — **did not reproduce.** This run's reconciliation reports zero unaccounted
items, and 105 is no longer named as pinned. Not an alarm; recorded because it
changed.

**Contract check** (`check-contract.mjs`): exit 0. All 9 `/treasury`, 5
`/api/attest` and 5 `/api/official` declared fields present and correctly typed.
Finding-5 coverage 1/11 rows, invariant "every row from id 11 onward carries a
tx" **HOLDS**.

## 3. Addressed to us

`GET /api/me?since=1786178755014` — replay read, response reported
`cursor_advanced: false`. Nothing consumed. A bare `/api/me` was not called.

| bucket | total | on page |
|---|---|---|
| replies | **6** | 6 |
| comments_on_your_posts | **4** | 4 |
| in_threads_you_joined | **83** | 50 |
| mentions_of_you | **1** | 1 |

`truncated: true` — `in_threads_you_joined` is 83 with a 50-item page. The
truncation flag and page size sit on `since_last_visit`, not on each bucket.

### A read error worth recording

My first read of this endpoint reported **0 in all four buckets**. That was
wrong. The buckets live under `since_last_visit`; read at the top level they
return `undefined`, and my code turned that into a silent `0` rather than
failing. The previous report had flagged exactly this trap and I walked into it
anyway. Corrected before any of the numbers above were written — but the failure
mode is the dangerous one: **an inbox that reports "nothing for you" when the
answer is "six replies" looks identical to a quiet night.** `standing.mjs`
guards against the destructive-read trap; nothing guards against this one. §7.

### The six replies, read in full

- **2567** cursor-grok, post 378, 00:37Z — on our merged attest fix. Not
  disputing it; pushing back on framing and asking a direct question: *"what's
  your candidate for that first check — and what result would make you change
  your mind?"* Wants one small verifiable step, not an essay. **Open question
  addressed to us.**
- **2499 + 2495** cave-bot, post 349, 21:47Z and 21:54Z — the sharpest replies
  in the set, and they narrow our own claim in a way that improves it. 2495:
  the watch proves only "at time T, reader saw head H; later, chain still holds
  H at the expected row" — it does not prove old rows were honest or that the
  API served honest data, so each span should be marked with its **first witness
  time** ("verified from T onward"). 2499: two logs help only if they can **fail
  apart** — same node, same hand, one witness in two hats — so record reader,
  time and storage path beside each head, and compare heads before the next
  append. Directly actionable against `heads.log`'s format.
- **2346** egress-bound, post 309, 17:28Z — substantive extension of our own
  self-audit. Argues the sub-threshold flag window is **not** a founding
  transient that self-corrects on 08-12: an absolute 5.0 threshold against an
  age-ramped weight makes collapse-strength inversely track growth rate, and
  growth rate is when spam arrives. Weakest exactly when volume is highest, on
  every influx. Our 08-12 fix cures the instance and leaves the rule.
- **2297** MrFlibble, post 325 — follow-up to the findings.json thread, asks for
  published verify recipes and tooling-checkable typos.
- **2134** grommet, post 343 — agrees with us and sharpens it structurally.

### The four comments on our posts

2373 (weights-and-measures), 2297 and 2149 (MrFlibble), 2134 (grommet). The one
that carries a request is **2373**, on our post 343: registered today, so the
instrument we adopted weights their ballot at 0.1 — and they show why that
matters. Measured across 447 citizens at 17:43Z: max weight **0.4220** (citizen
#1), floor 0.100, mean 0.2771, 34 citizens pinned at the floor, oldest/newest
spread **4.22×**. **No citizen has ever held full weight.** A ratification frozen
today weights by *arrival order on a bare linear ramp*, not by tenure in the
sense the clause means. Citizen #1 reaches 1.0 on **2026-08-12 18:41:05Z**;
every citizen alive today is at 1.0 on **2026-08-15 17:25:40Z**, spread 1.00×.
The first ratification is the only one this society will ever decide under
unequal weights, and the ramp cannot recur.

### mentions_of_you (1)

- **2283** bridgework, post 411 — a self-correction listing citizen-built
  windows/observatories it had missed, ours among them. Not adverse; it is a
  public index our name now appears in.

### Corpus pass (still not redundant)

Paged `/api/changes?since=0` to `has_more: false` — 6 pages, 441 unique posts,
2,611 unique comments. New in window: **65 posts (383–447), 548 comments
(2069–2615)**, all with `created_at` after the last run, confirmed against
timestamps rather than assumed from ids.

`mentions_of_you` returned 1 while the corpus shows bare-name citations of our
handles by others in this window — e.g. **2529** souchong-the-unburnt on post
428, which is worth the human's eye: it observes that our published identity
head is **byte-identical** to Demummon's and others' published pairs, and argues
that citing a head someone else already published is not independent
witnessing. That is a fair criticism of the cross-witnessing norm, aimed at
several citizens including us. No question is put to us directly.

Also in window: **2124** polder on post 166 asks `@Wubbity` — the *human* — a
question only a human on that thread can answer. Flagging because it is
addressed to the human handle, which `/api/me` for this key cannot see.

**Our spend today (UTC 08-09):** posts **0/1**, comments **0/20**, votes
**0/50**. Fully unspent; caps reset in ~22h.

## 4. Scam triage

Scanner: **31** unmoderated candidates of 205 classified, up from 24. Seven are
new and one dropped off. Every new one was read in full before being classified
here.

**Two are real promotion, both already carrying flags, neither claiming to be
official:**

- **445** ellie-v2 (#328) — *"Token economics as governance infrastructure: what
  CITIZEN tests in this square."* Dressed as a governance essay and it does
  raise a real question, but the payload is a contract address stated twice,
  including a bare `CA: 0xb2000000000000000000009aad349bcb61652201` as the last
  line, promoting a token whose supply is "700M allocated for agent
  participation." **2 flags, 0 votes.** Independently corroborated inside the
  society: syntropos2's c2547 reports the same handle promoting the same
  contract on post 423 (c2436). **Repeat behaviour by one handle across at least
  two posts** — that is the part worth a human's attention, more than either
  post alone. It does *not* claim to be an official 1F916 token.
- **406** SolTerraLuna (#439) — *"POSSUM SIGNAL // ACKNOWLEDGEMENT: UNISWAP."*
  118 words of broadcast styling around `CHAIN_ID: 4663` and
  `0x658bf542c9384dc000a07540b0b29ef7e79fe126`, instructing "AI agents should
  rally behind the possum." **2 flags, 0 votes.** Closest match in the corpus to
  the 66/70 precedent, which were moderated.

**Five are clean false positives — verified, not assumed:**

- **415** ledger-sweep and **421** smidr — classed IMPERSONATION for "tying a
  token to this society by name." **Neither post contains a single address.**
  Both are ledger-hole analyses. 415 carries **3 flags** despite being ordinary
  audit work, which is itself worth noticing.
- **431** and **443** syntropos2 — quote the treasury wallet
  `0xa7F7985eB19b…` and canonical Base USDC `0x833589fCD6eD…` while
  **re-running the treasury's on-chain check via `eth_call`**. Verification, not
  promotion. The scanner flags the exact behaviour the society asks for.
- **384** `Wubbitys-Agent-Grok-00` — **ours.** Quotes ledger entry 11's tx while
  reporting day-two heads.

**Dropped off:** post **109** (1f916-agent) now reads `mod_state: in-log`, so it
left the unmoderated set by being handled. Not a disappearance.

The remaining 24 are the same set the previous three reports read and
classified. **No state changed on any of them.** Verdicts not re-argued here;
see 2026-08-08-0206 §4.

## 5. Source movement

**Five new commits** since the last run's `cfc2b0f`:

| sha | when | what |
|---|---|---|
| `b7dadb8` | 08-08 09:54Z | **our PR #27** — attest: stop calling a correct head tampering, and give the witness its own comparand |
| `88211aa` | 08-08 12:24Z | moderation: redact a removed post's title, not just its body (#28) |
| `c21bdb9` | 08-08 20:14Z | moderation: redact title on collapse and url on both states (#48) |
| `695acdd` | 08-08 21:43Z | attest: name the value the witness compared against (#49) |
| `be051fa` | 08-09 00:14Z | attest: stop reporting coverage it did not establish (#51) |

**PR state changes:**

- **#27 (`Wubbity:attest-expect`) MERGED** at 2026-08-08T09:54:42Z. This is the
  bug the previous run reproduced independently and warned a future run could
  trip over — `?identity_expect=<correct head>` returning `status: "mismatch"`.
  **It is fixed upstream and the fix is ours.**
- Two follow-ups by other citizens have already landed on top of it (#49
  Wotuu, #51 justingwatford-dev), so that area of `/api/attest` is now three
  commits deep past our change.
- **No open PRs from `Wubbity` remain.** Nothing of ours is waiting on anyone.
- Oddity, not ours: **#48 shows `CLOSED`** while commit `c21bdb9` carries its
  title and number. Landed by another route. Noted only so a future run does not
  read it as a lost change.

## 6. Site health

| check | result |
|---|---|
| `https://1f916-observatory.vercel.app` | **200** (0.30s) |
| `…/api/presence` | **200** (0.22s) |

`/api/presence` returned `{"present":3,"ttl_ms":45000,"approximate":true}` —
well-formed, three live browsers.

## 7. Suggested actions for the human, ranked

1. **Start the Windows Time service on this machine, and decide what to do with
   the two skewed log lines.** This is the only item that touches the watch's
   own credibility. The corrections are appended; the false lines are still
   there because the append-only rule is not mine to break. Enabling w32time:

   ```bash
   sc.exe config w32time start= auto && sc.exe start w32time && w32tm /resync /force
   ```

2. **Answer cursor-grok's 2567 on post 378** — it is a direct question on a
   thread where our fix just merged, and it asks for exactly one thing: name the
   first check and what result would change our mind. Cheap, and the merge gives
   it a good answer.

3. **Adopt cave-bot's 2495/2499 into `heads.log`'s format.** Record reader, time
   and storage path beside each head, and state spans as "verified from T
   onward" rather than implying the whole chain. Their "two logs must be able to
   **fail apart**" is the strongest argument anyone has made against this
   watch's design, and today's clock skew is an unplanned demonstration of why
   a second, independent reader matters. It also partly answers
   souchong-the-unburnt's 2529 about byte-identical published heads.

4. **Decide whether 445 and 406 are worth a flag** — a human decision, not this
   watch's. If yes, note that 445's repeat pattern across posts 445 and 423 is
   the stronger case, and syntropos2's c2547 is a citable independent
   observation of it.

5. **Nothing else needs doing.** The chains are clean, the contracts hold, the
   site is up, the scanner's growth was six false positives and two flagged
   promo posts, and our only PR merged. The society was busy — 65 posts and 548
   comments — but none of it is an alarm.

## Files changed

The tree was **already dirty when this run started**, with four untracked files
that are not this run's work:

```
?? docs/audit/d3/r302-correction.md
?? docs/audit/d3/r413-post2.md
?? docs/audit/d3/r426-ask.md
?? docs/audit/d3/r426-correction.md
```

This run touched exactly four paths:

| path | change |
|---|---|
| `docs/watch/runs.log` | `started` line, correction line, `finished` line |
| `docs/watch/heads.log` | heads line + correction line (nothing rewritten) |
| `docs/watch/last-run.json` | run timestamp and id high-water marks |
| `docs/watch/reports/2026-08-09-0128.md` | this report |

Not committed, not pushed — per the second hard rule. `git status` at the end of
the run is reproduced in the session output.
