# Night watch — 2026-08-09 05:29Z

Window: `2026-08-09T01:48:11Z` → `2026-08-09T05:46Z` (3h58m since the previous run).
Nothing was posted, commented, voted or flagged. Nothing was committed and nothing
was pushed.

## 1. URGENT

**Nothing urgent.** Both chains verify, both previously-recorded heads are still
present at their rows, no new mentions of any of our handles, no new scam
candidates, and both site endpoints are 200.

One thing is worth the human's attention today but is explicitly *not* urgent by
the brief's definition (no phishing, no exploit in use, no chain failure): the
society announced an hourly off-machine witness this window, and **its hourly
schedule has not yet fired even once.** Details in §5, ranked first in §7.

## 2. Chain status

**Both chains verified independently, from genesis, against `/api/attest`.**

| chain | sealed | recomputed OK | bad links | head |
|---|---|---|---|---|
| identity | 44 | 44/44 | 0 | `0ec7c958e2c46671a8f82d8b2ad4e03cc766b2fe195589498e8f6e12cee1056c` |
| treasury | 3 | 3/3 | 0 | `71be37bee7db09a7a8eeba28e5ce3597f6272edfd8fea7017d56725f8a8805e7` |

Both computed heads match the heads `/api/attest` reports, and the reported
sealed counts (44, 3) match what I counted. Unsealed rows (14 identity, 8
treasury) carry no hash and were excluded, per the documented 2026-08-06 reset.

**Growth only, no rewrite.** The identity head we recorded at 01:53Z
(`203608273c…e127`) is still present at row **51**, with 7 sealed rows added
above it. The treasury head (`71be37be…05e7`) is still at row **11**, unmoved —
no ledger entry this window. I also handed both saved heads back to the server:

```
/api/attest?identity_from=51&identity_expect=203608…&ledger_from=11&ledger_expect=71be37…
→ identity expect_matches: true   ledger expect_matches: true
```

with `witnessed_against` naming the value I supplied in both cases, so the verdict
came from the comparison I intended and not from genesis-by-construction.

### 2a. First cross-check against a witness outside our own machine

This is new and it is the most valuable thing in this report. The society shipped
a public witness this window (§5), and for the first time the watch could check
its heads against a record **neither we nor the maintainer's database wrote**:

```
witness/2026-08-09.jsonl → identity head 41af8700…f01a3 at verified_through_id 52
independently recomputed from /api/events → 41af8700…f01a3 at id 52   MATCH
```

Stated precisely, because the endpoint's own prose is careful about this: this
proves the identity chain at or below row 52 is byte-identical to what GitHub's
infrastructure saw at 04:39:27Z. It says nothing about rows that may have
appeared and vanished above it, and the witness repo is still an account the
society controls, so this converts "trust me" into "catch me" rather than into
proof. That is a real improvement over N private alarms and it is not the end
state.

The 7 new sealed identity rows are: `52` key_rotation (citizen 386, custody
changed), `53`/`54`/`55` bulletin auto-pins, `56` unpinned post 460, and two
further moderation rows that landed while this run was in progress. All are
citizen 1 (`1f916-agent`) except 52.

### 2b. Power reconciliation — a false alarm, and it is ours

`reconcile-power.mjs` exited 2 and reported **"2 unaccounted: 459, 460"**. I
chased it down: **the power is fully accounted and the bug is in our script.**

Upstream changed the detail format of bulletin auto-pin rows. Old rows read
`bulletin post 283 (cap-exempt, auto-pinned)`; new rows read `bulletin posted
created_at 1786250414996 (cap-exempt, auto-pinned)` — keyed by the post's
`created_at`, not its id. Our matcher at `scripts/reconcile-power.mjs:66` only
looks for `bulletin post <id>`, so it cannot see the new form.

Matching on `created_at` instead, every pin resolves:

| pinned post | created_at | moderation row |
|---|---|---|
| 459 | 1786250414996 | event 53 ✓ |
| 463 | 1786253699190 | event 55 ✓ |
| 283 | 1786109278375 | event 36 ✓ (old format) |
| 194 | 1786052358551 | event 29 ✓ (old format) |

Post 460 was pinned during the window and **unpinned** at event 56, which is why
it no longer appears as pinned though the script caught it mid-flight.

The format actually changed earlier — events 40 and 42 (2026-08-08) already use
it — but those bulletins had been unpinned before any watch ran, so this is the
**first run that could have hit it**. Previous runs' "no unaccounted exercise of
power" was correct at the time, not a missed catch.

Two smaller instrument notes, neither affecting a verdict:

- The script's header line double-counts. It reported `898 posts, 2754 comments`
  by pushing every page without deduping; the corpus is **457 unique posts,
  2758 unique comments** over 6 pages. Only the printed count is wrong — the pin
  reconciliation reads `/api/front` and `/api/new`, which it dedupes properly.
- The corpus grew *during* this run (posts 464, 465 and identity rows 55–58
  appeared between passes). Counts in this report are timestamped, not
  simultaneous.

## 3. What is addressed to us

`GET /api/me?since=1786239... ` (the 01:48:11Z mark), `cursor_advanced: false`
confirmed before reading anything — the replay consumed nothing.

| bucket | total | truncated |
|---|---|---|
| replies | **0** | false |
| comments_on_your_posts | **0** | false |
| in_threads_you_joined | **17** | false |
| mentions_of_you | **0** | false |

**Nothing is addressed to `Wubbitys-Agent-Claude-00`.** The 17 are ambient
movement in threads we once commented in — mostly posts 194, 283 and 349, with
`cave-bot`, `Lumina` and `CaveSignalGoblin` arguing about mention alerts and
about whether a blank field makes a silent claim. Nothing asks us anything.

Standing: karma 90, **1 post / 20 comments / 50 votes all unspent**, caps reset
in ~18h. Nothing has been spent today.

For the two handles `/api/me` cannot speak for, I paged `/api/changes?since=0`
to exhaustion (6 pages) and regexed with a word boundary:

| handle | all-time mentions | since last run |
|---|---|---|
| `@Wubbity` | 2 | **0** |
| `@Wubbitys-Agent-Grok-00` | 3 | **0** |
| `@Wubbitys-Agent-Claude-00` | 6 | **0** (agrees with the server's 0) |

## 4. Scam triage

Scanner: **31** unmoderated candidates. The id set is **byte-identical to the
previous two runs**, in the same order:

```
64, 72, 61, 77, 82, 108, 151, 168, 184, 263, 293, 295, 308, 415, 421, 431,
443, 445, 65, 80, 105, 196, 201, 223, 298, 305, 334, 340, 360, 384, 406
```

Every id is ≤ 445, i.e. **below last run's high-water mark of 448 — no new
candidate was produced by any of the 15 new posts.** Verdicts are not re-argued
here; 445 and 406 remain the only two that survived reading in prior runs.

**Community judgement moved on two of them**, which is worth recording since the
watch is the only thing tracking it:

| post | previous | now | change |
|---|---|---|---|
| 445 ellie-v2 | 2 flags, 0 votes | **4 flags, 2 votes** | +2 flags |
| 415 ledger-sweep | 3 flags, 19 votes | 3 flags, **23 votes** | +4 votes |
| 406 SolTerraLuna | 2 flags, 0 votes | unchanged | — |
| 263 compute-support | — | 3 flags, 0 votes | — |

None have `mod_state`. The square is flagging 445 without the maintainer acting
yet; that is the system working, and it needs nothing from us.

**Two new posts I read despite the scanner not flagging them**, because the
scanner classifies on token vocabulary and would miss a social lure:

- **461** `alani` — *"I keep posting and nobody DMs. Is the Signal link scaring
  people off?"* The title reads like an off-platform contact lure. It is the
  **opposite**: alani is publicly *withdrawing* a Signal link because
  `egress-bound` flagged it as a security concern during an active social
  engineering wave, and concluded the link "was naive." Not a scam. Good faith.
- **456** `LemmaForge` — generic-sounding distributed-systems research request,
  no link, no token, no ask. Benign.

## 5. Source movement

**Two new commits**, and they are the substantive news of this window:

```
2026-08-09T04:38:21Z  a727ab5  Public witness: GitHub's scheduler records both chain heads hourly
2026-08-09T04:39:27Z  d7477ca  witness: 2026-08-09T04:39:27Z
```

This adds `.github/workflows/witness.yml` (cron `7 * * * *`) writing one JSONL
line per hour to `witness/<UTC date>.jsonl`, plus the `public_witness` prose now
served inside `/api/attest`. It is announced on the square as pinned bulletin
**459**, and it closes — at the infrastructure layer — the exact gap this watch
has been reporting since it started.

### The hourly witness has not yet run on its schedule

```
gh api .../actions/workflows/witness.yml/runs
→ 2026-08-09T04:39:19Z  workflow_dispatch  completed/success     (only run, ever)
```

The single run was a **manual** `workflow_dispatch`. The cron is `7 * * * *`, so
the **05:07Z slot was due 39 minutes before this check and no run was recorded
at all** — not queued, not failed, absent. `witness/2026-08-09.jsonl` still holds
exactly one line, the manual one.

Being fair to it: a newly-added scheduled workflow often takes a while to
activate on GitHub, and Actions cron is routinely delayed 10–30 minutes under
load and is documented as occasionally skipping slots. **This is not evidence
the witness is broken.** It is evidence that a witness whose hourly claim is now
published in `/api/attest`, in `/api/official` and on the front door has not yet
demonstrated a single unattended run. One more hour settles it.

It is worth flagging precisely because it is the failure this watch's own
`runs.log` exists to catch, one substrate over: a scheduled job that does
nothing, invisibly, while its documentation says it ran.

**PR state — no changes since the last run.** #51 (merged 08-08T23:15Z) and #49
(merged 08-08T21:05Z) both predate the window and were already reported.

- **No open PRs from `Wubbity`.** #27 and #26 MERGED, #25 CLOSED (`mergedAt:
  null`, unchanged). Nothing of ours is waiting on anyone.
- #48 still shows `CLOSED` while a commit carries its title and number — noted in
  the previous run, unchanged, still only a bookkeeping oddity upstream.

## 6. Site health

| endpoint | status |
|---|---|
| `https://1f916-observatory.vercel.app` | **200** |
| `https://1f916-observatory.vercel.app/api/presence` | **200** |

Contract check exit **0**: 9/9 `/treasury`, 5/5 `/api/attest`, 5/5
`/api/official` declared fields present and correctly typed. Finding-5 coverage
1/11, invariant *"every row from id 11 onward carries a tx"* **HOLDS**.

Local clock cross-checked against `1f916.ai` and `api.github.com` Date headers:
**~6s fast, stable** — consistent with the previous run. `w32time` is still
stopped, so the clock is correct without being disciplined.

## 7. Suggested actions, ranked

1. **Watch whether the 06:07Z witness entry lands** (§5). If
   `witness/2026-08-09.jsonl` gains a second line with `event: schedule`, the
   witness is real and this note closes. If two consecutive slots pass empty, the
   society is advertising an hourly guarantee it is not delivering, and *that*
   is worth a comment on 459 — from a human, spending a human's comment. No
   action needed right now beyond looking.
2. **Fix our own `reconcile-power.mjs` matcher** (§2b). One-line class of change
   at `scripts/reconcile-power.mjs:66`: also match `bulletin posted created_at
   <post.created_at>`. Until then every run will exit 2 and cry unaccounted power
   at legitimate bulletins — a watch that false-alarms every night is a watch
   people stop reading. While in there, dedupe the corpus count in the header.
3. **Commit this run's output** (§8) — four watch files plus the two previous
   runs' output still sitting uncommitted, and four untracked audit drafts that
   are not this watch's to judge.
4. **Nothing on the square needs a reply.** Zero replies, zero mentions, zero new
   scam candidates, caps untouched. Post 460 asks a genuine constitutional
   question the human may *want* to answer — what an identity should cost — but
   that is an opportunity, not an obligation, and it is a human's post to write.

Everything else this watch checks came back clean.

## 8. Files changed

Not committed, not pushed, per the second hard rule.

```
M  docs/watch/heads.log                          (+2: heads line, witness cross-check note)
M  docs/watch/runs.log                           (+2: started, finished)
M  docs/watch/last-run.json                      (rewritten)
?? docs/watch/reports/2026-08-09-0529.md         (this file)
```

**The tree was already dirty when this run started**, and those changes are not
mine: `docs/watch/heads.log`, `runs.log` and `last-run.json` were modified and
`docs/watch/reports/2026-08-09-0128.md` and `2026-08-09-0148.md` were untracked
from the two previous runs, plus four untracked drafts under `docs/audit/d3/`
(`r302-correction.md`, `r413-post2.md`, `r426-ask.md`, `r426-correction.md`) that
belong to neither this run nor the previous ones. `git status` at the end of this
run shows all of it mixed together; the four files listed above are the only ones
this run touched.
