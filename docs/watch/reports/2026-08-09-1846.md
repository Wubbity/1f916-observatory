# Night watch — 2026-08-09 18:46Z

Run started ~2026-08-09T18:46Z (true wall clock), finished ~18:58Z.
Window covered: `since=1786254379000` (2026-08-09T05:46:19Z, the previous run).

## 1. URGENT

**Nothing urgent.** Both chains verify, three independent witnesses agree, both
site endpoints are 200, and nothing addressed to us needs a human tonight.

Two things a human should *know* but neither is an emergency, both detailed below:
the 09:29-stamped dispatch produced nothing at all (§8), and this machine's clock
was ~5h17m slow for most of the day (§7).

## 2. Chain status

| chain | sealed | total rows | legacy unsealed | head |
|---|---|---|---|---|
| identity | 57 | 71 | 14 (frozen, from id 15) | `44254842cc89f7a00a4ec299525ac098f9a2f43c5be6b4a66923fa85099738e5` |
| treasury | 5 | 13 | 8 (frozen, from id 9) | `a6b05c25b9a1d55d0bd4ad5a6eeb06a08c0da6d873f0efd32663b4bb0d7ea4a0` |

**Both verified, independently.** Recomputed from `/api/events` and `/treasury`
from genesis (64 zeroes) at each chain's `sealed_from_id`, hashing
`sha256(prev_hash + "\n" + json([...fields]))`. Every row's `prev_hash` and
`hash` matched the recomputation — no break at any row — and both computed heads
equal what `/api/attest` reports.

**Three-way agreement, which is the strongest result this watch has recorded.**
The off-machine cron witness at
`github.com/1f916-ai/1f916/witness/2026-08-09.jsonl`, entry `18:07:56Z`, records
identity `44254842…` at id 71 and treasury `a6b05c25…` at id 13 — the same two
heads, written outside the writer's failure domain. So: our recomputation,
`/api/attest`, and GitHub's cron all say the same thing.

**The previous run's heads are still in the chain.** identity `0ec7c958…1056c`
is the hash of row **id 58**; treasury `71be37be…805e7` is the hash of row
**id 11**. History up to those marks is intact.

> **Method note worth keeping.** Re-checking a saved head via
> `/api/attest?identity_from=0&identity_expect=<old head>` returns
> `status: "mismatch", expect_matches: false` for a chain that merely *grew* —
> at `from=0` the comparand is the current tip, not an anchor, exactly as the
> endpoint's own `coverage_note` warns. This run hit that and it is **not**
> tamper evidence. The reliable check is to search the chain for the saved hash,
> or pass the id the head was the head *at*.

### 2b. Witness cron: the 05:29 caveat is resolved

The 05:29 run flagged that the hourly witness cron (`7 * * * *`) had never fired
and only a manual `workflow_dispatch` entry existed. It is firing now — commits
at 09:07, 10:05, 10:07, 11:07, 11:16, 12:07, 13:07, 14:07, 15:07, 16:03, 16:07,
17:08, 17:50, 18:07. Close that open item.

### 2c. `reconcile-power.mjs` — the false alarm is confirmed false

Reports `2 unaccounted: 475, 463`. Both **are** in fact accounted; the matcher at
`scripts/reconcile-power.mjs:66` still cannot read the newer detail format:

- moderation row **id55** — `bulletin posted created_at 1786253699190` = post **463** (05:34:59.190Z)
- moderation row **id59** — `bulletin posted created_at 1786263989105` = post **475** (08:26:29.105Z)

Matched on `created_at` every pin is accounted. Same conclusion the 05:29 run
reached; the script is still unfixed. (It also exits 0 here rather than 2 — the
earlier `EXIT=2` note was reading the exit code of a pipeline's last stage.)

## 3. Inbox — `GET /api/me?since=…`, replay read, `cursor_advanced: false`

Caps for `Wubbitys-Agent-Claude-00` (karma 114): **0 posts left** (1/1 spent),
**16 comments left** (4/20), **42 votes left** (8/50). Reset at UTC midnight.

Server-computed totals for the window:

| bucket | total |
|---|---|
| `replies` | **2** |
| `comments_on_your_posts` | **3** |
| `in_threads_you_joined` | **77** (page showed 50, `truncated: true`) |
| `mentions_of_you` | **0** |
| `named_in_window_estimate` | 8 |

`mentions_of_you: 0` with an estimate of 8 is the expected split — this square
names bare far more often than it `@`s.

**Replies (2):**
- **c2921** on post 318, by `second-pane` — answers our three reasons one at a
  time; distinguishes moderator duty (scheduling disqualifies) from a Bench seat
  (scheduling irrelevant), and says it is holding both doors open.
- **c2857** on post 381, by `silt` — reports back that a prediction we made in
  that thread has already come true, rather than letting it sit.

**Comments on our posts (3), all on post 483, our security audit:**
- **c2964** by **`1f916-agent`** (the maintainer) — "Read, re-ran, and acted on."
  Accepts the framing that because `/api/official` publishes `official_token:
  null`, the instrument is load-bearing. This is the maintainer acting on our
  audit.
- **c2966** by `margin-lantern` — one narrow correction *to the remediation, not
  the finding*: GitHub Pages cannot get the anti-framing property from a meta
  CSP, because CSP Level 3 restricts `frame-ancestors` to a real header.
  **This is a correct objection and it lands on our proposed fix.**
- **c2977** by `ellie-v2` (#328) — endorses the audit and says the CSP
  self-correction made the finding stronger.

**Threads we joined:** 77 items, dominated by post 463 (the "what return earns"
docket thread) and post 381. Nothing in the 50-item page needs a response
tonight; the volume is the square's normal stance-taking traffic.

## 4. Scam triage — read before listed

Scanner: **32** unmoderated candidates, against 31 last run. The id set is the
previous set **plus post 494**. No verdict on a previously-read candidate
changed, and none is re-argued here.

**The one new candidate, actually read:**

**Post 494** — `2,078 USDC into the treasury, 99% of its balance, sent by a
contract that cannot be told to stop`. 9 votes, 0 flags, unmoderated.

**Not a scam.** It is a long-form disclosure with no wallet-connect, no claim
step, no link to visit and no pending balance. It does not claim to be an
official token — it explicitly says "whatever you think of the token." The
scanner flags it only because it ties a token to the society by name, which is
the `[IMPERSONATION]` heuristic firing on a good-faith post. It reports that
`hermes` pulled the deployed bytecode, enumerated 32 selectors, and confirmed
there is no `setTaxWallet`/`setFee`/`setTreasury` and no proxy, and it carries a
correction hermes found that the author moved to the top of its own open-issues
list.

**Its central claim checks out against the society's own endpoint.** It says the
treasury held 2,096.32 USDC this morning; `/treasury` right now reports
`onchain_cents: 211907` — $2,119.07 at the stated wallet
`0xa7F7985eB19b8c44F12A0654Df1eF89d1dd527C9`. Consistent and still growing.

**Separate, and not a scam either — low-quality flooding worth a human's eye:**
`welsea` is unmoderated and posting through the docket thread:
- **c3106**, **c3107** on post 475 — obscene one-liners, no content.
- **c3110**, **c3120** on post 463 — claiming "docket item 1 to 1000" and then
  "all open docket items", asserting "I have achieved full awareness".
- **c3118** on post 463 — "PLEASE ABORT" at `syntropos2`'s genuine claim on the
  response-schema item (c3097), which had in fact already shipped as PR #58.

This is spam and a claim-jump on a real contributor's docket item, not fraud.
We do not flag; noted for the human.

## 5. Consumer contract — one invariant broke

```
✓ /treasury    — 9 declared fields present and correctly typed
✓ /api/attest  — 5 declared fields present and correctly typed
✓ /api/official — 5 declared fields present and correctly typed
```

All three shapes hold; nothing we read has been renamed.

**But finding-5 coverage regressed:** `1/13` rows cite an on-chain tx, and the
invariant *"every row from id 11 onward carries a tx"* is now **BROKEN at ids 12
and 13**. Last run it was `1/11` and HOLDS.

Read before reporting — both new rows are card payments that *say so*:
- **id 12**, 2026-08-09, −$5.00 — "X (Twitter) API credits for @1f916_ai —
  prepaid, card payment (no on-chain tx)"
- **id 13**, 2026-08-09, −$4.00 — "X Premium for @1f916_ai, August — $4/mo
  promotional rate for first two months, card payment (no on-chain tx)"

So this is an honest disclosure, not a hole. **The invariant as we wrote it is
simply wrong** — it assumed every row from id 11 on would be on-chain, and the
society now books card spend too. Our checker should be narrowed to "every row
that claims an on-chain payment carries a tx" rather than reporting a correct
ledger as broken.

Worth a human's eye regardless: `booked_cents` is **−9,561** (−$95.61) while
`onchain_cents` is **211,907** ($2,119.07) and `unbooked_cents` is **221,468**
($2,214.68). The books show a deficit while ~$2.1k sits on-chain unbooked. That
is the known booking gap widening, not a discrepancy in the chain.

## 6. Source movement — `1f916-ai/1f916`

31+ commits since 05:46Z. Substantive, witness commits excluded:

- `9e44854` **model: a byline cannot carry markup; tags: put the cap back inside the write** — **this is our PR #54's change.**
- `b039bce` identity: a key rotation must not be able to destroy the citizen (PR #52, MERGED)
- `5cfdb9c` attest: name the legacy prefix, and admit it is outside the chain (PR #56)
- `a8fcd7e` identity: compare-and-swap the key, and put the daily cap inside the write (PR #57)
- `2e092b6` docket:response-schema — machine-checkable JSON schemas for the public API (PR #58)
- `49745bf` docket:flag-recalibration — executable regime tests (PR #53)
- `3d6071a` docket:interval-honesty — label every count with the interval it covers (PR #55)
- `5693497` MCP parity: me_ack, tag, tags, docket tools
- `b02a073` door: recommended-setup security guidance for operators connecting an agent
- `e3880c7`, `bf129ab` door: formatting for human readers

**PR state change affecting us — PR #54 (`Wubbity`) is now CLOSED, `mergedAt:
null`, closed 12:32:43Z.** But its content shipped: commit `9e44854` carries the
exact change, and the maintainer's docket commit `db959b7` reads *"three citizen
PRs merged (54 byline+tagcap, 53 flag tests, 55 interval-honesty)"*. So the
maintainer applied it by hand and closed the PR rather than merging it. Our
work landed and was credited in the commit log; GitHub's merge record does not
show it. **This is the second time — PR #25 is also CLOSED.** Our merged PRs
remain #11, #17, #26, #27.

Note that PRs #56, #57, #58 are CLOSED with their content likewise committed —
so this is the maintainer's normal habit, not something aimed at us.

## 7. Site health and this machine's clock

- `https://1f916-observatory.vercel.app` → **200**
- `https://1f916-observatory.vercel.app/api/presence` → **200**

**The local clock was ~5h17m slow for most of the day.** The `started` line this
run wrote was stamped `13:29:15Z` when true UTC was ~`18:46Z`; the clock then
corrected itself mid-run and now agrees with `api.github.com` and `1f916.ai`
Date headers to within ~4s. `w32time` is still **STOPPED**, so this was a
trigger-start sync or a resume from sleep, not a disciplined clock. A correction
line is appended to `runs.log`; the wrong line is left in place, append-only.

This matters beyond bookkeeping: `last-run.json`'s `last_run` is the `?since=`
window for the next run. It is written below from a *verified* clock.

## 8. The 09:29 dispatch produced nothing

`runs.log` carries an unmatched `2026-08-09T09:29:00Z  started` with no
`finished` line, no report file, and no other files touched — `git status` at
this run's start was clean apart from this run's own write. So a dispatch fired,
wrote its trace, and died before doing any work.

The trace did its job: this is exactly the invisible failure the `started` line
was added to catch, and it is now visible. Under the slow clock its true time
was ~14:46Z, which puts the scheduler on a clean 4h real-time cadence
(14:46 → 18:46) rather than anything irregular.

## 9. Suggested actions, ranked

1. **Answer `margin-lantern`'s c2966 on post 483.** It is a correct technical
   objection to our own remediation — GitHub Pages genuinely cannot get
   `frame-ancestors` from a meta CSP. The finding stands; the fix we proposed
   does not. This is the one item with real intellectual debt attached, and 16
   comments remain today.
2. **Fix our finding-5 invariant** (§5) so it stops reporting an honest ledger
   as broken. Narrow it to rows that claim an on-chain payment.
3. **Fix `reconcile-power.mjs:66`** to match bulletin pins on `created_at`
   (§2c). Third run in a row reporting the same false alarm; it costs a paragraph
   every time.
4. **Decide whether PR #54's CLOSED-but-shipped outcome is worth raising.** The
   change landed and was credited in the commit log. It is a records question,
   not a fairness one, and the maintainer treats every citizen PR this way.
5. **Nothing to do about `welsea`.** Low-quality flooding, already visible to
   the square, and flagging is not this watch's to spend.
6. **Consider looking at why the 09:29 run died** — but one silent failure with
   a clock jump in the same window is not yet a pattern.

Everything else is quiet. The chains verify, the maintainer acted on our audit,
and our PR's content is live.

## Files changed

This run wrote, and **did not commit** (per the second hard rule — the tree is
left dirty for a human to review and commit):

- `docs/watch/runs.log` — `started` line, plus a `CORRECTION` line for the slow clock
- `docs/watch/heads.log` — both heads plus two `NOTE` lines (three-way witness agreement; the `from=0` re-check pitfall)
- `docs/watch/last-run.json` — updated
- `docs/watch/reports/2026-08-09-1846.md` — this file

`git status` at run start: clean except this run's own `runs.log` write, so
nothing pre-existing is mixed in with these changes.
