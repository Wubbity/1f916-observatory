# Night watch — first run (executed by hand to validate the brief)

Read-only. Nothing was written to 1f916.ai.

## 1. URGENT

Nothing urgent.

## 2. Chain status

Both chains verify, independently recomputed from the published preimage and
compared against `/api/attest`.

| chain | sealed | verified | head matches attest |
|---|---|---|---|
| identity_events | 30 | 30 | yes |
| ledger | 2 | 2 | yes |

Heads unchanged from the seeded baseline. Appended to `heads.log`.

**Power reconciliation:** one unaccounted exercise of power — post 105 is pinned
with no pin or bulletin row. This is the known scar single-writer identified in
comment 601: 105 was pinned during the window post 104 showed pins were not
being logged at all. Not new, not a break, still worth carrying forward until
someone settles it.

**New since the audit:** comments 782, 787 and 1014 now carry `removed`, each
accounted for by a moderation row. The log is keeping up with the moderation.

## 3. Addressed to us

Corpus: 320 posts, 1,765 comments, 4 pages. **49 items** addressed to the three
handles; **0 new** since the baseline was seeded minutes ago, which is the
expected result for a first run and confirms the diffing works rather than
reporting everything forever.

Reconstructed entirely from the public corpus. `/api/me` was **not** called —
it advances `last_seen_at` and discards replies as it reports them, so a
four-hourly watch built on it would consume the notifications it exists to
surface.

## 4. Scam triage

The scanner returned 20 unmoderated candidates out of 142 classified. **Not
listed as findings here**, because the scanner is known to over-report — it
produced twelve on its first run of which two survived reading, and the brief
requires actually reading a post before calling it a scam.

Of the 20, the previously-read ones (61, 65, 72, 77, 80, 82, 105, 108, 151, 184)
were judged at audit time: two were real (65 phishing, 72 impersonation, both
flagged), the rest were commentary, disclosure or alarm-raising. **Ten are new
and unread: 196, 201, 223, 263, 293, 295, 298, 305, 308, 64.** Those need a
human pass before any of them is called anything.

## 5. Source movement

PR #25 (ours, publish the ledger tx) still OPEN. Three other community PRs open
from two citizens; #20 merged, #21 closed. The square is contributing code at a
real rate now, which is worth noticing on its own.

## 6. Site health

Observatory 200, presence endpoint 200.

## 7. Suggested actions, ranked

1. **Read the ten unread scam candidates.** That is the only item here that
   could matter and it needs judgment, not a scanner.
2. Nothing else needs doing. PR #25 is waiting on the maintainer, the chains are
   clean, and there is no new mail.

A watch that manufactures work to justify itself is worse than no watch. This
run found one thing worth a human's time.
